# 운영 검증 기록 — 2026-09-13

대상: https://www.yttm.kr

사용자가 운영 배포 완료를 알린 뒤 수행한 부분 스모크 검증이다. 초기에는 운영 배포 SHA와 Firebase 설정을 대조하지 않았으며, 이후 확인한 배포·설정 정보는 아래 별도 항목에 기록한다.

## 1차 확인 완료 — 조회 중심

- 쿠키 없는 HTTP 요청에서 `/`, `/login`, `/en/login`이 200으로 응답했다.
- 쿠키 없는 `/dashboard`, `/admin` 요청은 307과 `/ko/login` 위치로 응답했다.
- 비인증 관리자 사용자 API 요청은 403, YouTube 영상 정보·검색 API 요청은 401로 차단됐다. 관리자 API에는 실제 사용자 UID 대신 존재하지 않는 테스트 문자열을 사용했다.
- Chrome에 이미 로그인되어 있던 계정으로 `/login` 진입 시 대시보드로 이동했고, 기존 영상·컬렉션을 조회했다. 새 Google OAuth 로그인 성공을 검증한 것은 아니다.
- 같은 기존 세션에서 `/admin` 화면과 통계를 조회했다. 비관리자 로그인 계정의 접근 거부와 `ADMIN_UID` 환경변수 값 자체는 확인하지 않았다.
- 영상 추가 대화상자에서 `YouTube Developers iframe API`를 검색해 실제 결과 8개가 표시됐다. 추가 버튼은 누르지 않았다.
- 랜딩 페이지에 공개된 데모 공유 링크에서 영상과 타임라인이 표시됐다. 1:27 메모를 클릭한 후 실제 YouTube 영상 재생 및 이후 1:39 재생 시각을 확인했다. 프레임 단위 이동 정확도나 비로그인 브라우저에서의 공유 페이지 동작까지 검증한 것은 아니다.
- Chrome 뷰포트 412×839에서 대시보드의 문서 폭과 화면 폭이 모두 412px였다. 검사 후 뷰포트 설정을 복원했다. 모바일 실기기·Safari 검증은 아니다.
- `/robots.txt`, `/sitemap.xml`이 200으로 응답했고 sitemap 주소는 운영 도메인을 사용했다.

## 1차 변경 범위

기존 로그인 상태를 유지했다. 영상·메모·컬렉션 생성/수정/삭제, 계정 생성/이관, 지표 동의 변경, 권한·배포 설정 변경은 수행하지 않았다. 검색 요청으로 서버 호출 제한 카운터나 캐시가 갱신될 수 있다. 개인 식별 정보와 관리자 사용자 목록은 이 기록에 저장하지 않는다.

## 2차 확인 완료 — 사용자 승인 후 저장·재로그인·확장 조회

사용자가 현재 로그인 계정에 테스트 영상·메모를 생성하는 것을 승인했다.

- 실제 URL 입력으로 YouTube 개발자 영상 `M7lc1UVf-VE`를 등록했다. 제목과 길이 22:24가 실제 메타데이터로 표시됐다.
- 생성한 영상: `/videos/f3yzzSozalbvzWDBRDH0`.
- 영상을 재생하고 현재 시각을 캡처해 0:07에 `[운영 검증 2026-09-13] 웹 저장·새로고침·확장 동기화 확인용 테스트 메모`를 저장했다.
- 새로고침 후 동일한 메모 본문·시각과 타임라인 1개 표시를 확인했다.
- 로그아웃 후 해당 영상 URL에 재접근하면 `/login`으로 이동했다.
- `Google로 계속하기`를 눌러 Firebase 인증 팝업을 거쳐 같은 계정으로 대시보드에 복귀했다. 브라우저의 기존 Google 세션을 사용한 재인증이며, 비밀번호 재입력·최초 동의·MFA 절차를 검증한 것은 아니다.
- 재로그인 후 같은 영상 페이지에서 동일한 메모를 확인했다.
- 설치된 Chrome 확장프로그램의 메모 사이드패널에서 같은 영상 제목과 0:07 테스트 메모가 표시됐다. 웹 → 설치된 확장프로그램 방향의 데이터 조회를 확인했다. 갱신 지연 시간까지 측정한 실시간 동기화 검증은 아니다.

### 2차 당시 미완료와 제약

- 확장 → 웹 역방향 저장은 확인하지 못했다. Chrome 창 전환 감지로 입력이 중단되었으며, 마지막 확인 화면에서는 확장 입력란이 비어 있고 저장 버튼이 비활성 상태였다. 이어서 네이티브 창 접근 오류가 발생했다.
- 동일 확장 화면을 별도 탭으로 열려는 시도도 브라우저 도구의 URL 정책에서 차단되어 중단했다. 제한을 우회하지 않았다. 이 도구 제약만으로 제품 결함이라고 판단하지 않는다.
- 이후 사용자가 직접 추가한 데이터로 역방향 반영을 확인했다. 아래 후속 검증을 참고한다.

### 남겨 둔 테스트 데이터

위 영상 1개와 웹에서 작성한 메모 1개를 유지했다. 기존 영상·메모·컬렉션은 변경하지 않았고, 공유 링크 생성이나 지표 동의 변경도 하지 않았다. 테스트 데이터 삭제는 수행하지 않았다. 로그인 상태는 같은 계정으로 복원했다.

## 후속 확인 — 확장 저장 결과의 웹 반영

사용자가 확장프로그램에서 새로 추가했다고 알린 뒤, 웹 대시보드에서 새 영상 `아스트로엔 통쟁 - 애 vs 벨 (astron war tower ac vs bel)`과 해당 영상의 0:16 `교전` 메모 1개를 확인했다. 영상 경로는 `/videos/sYo20rsOyS8RcemsgO7R`이다. 영상 페이지 새로고침 후에도 같은 메모가 유지됐다.

확장 저장 행위는 사용자 보고에 근거하고, 웹 반영·새로고침 유지는 직접 관찰했다. 이 범위에서 확장 → 웹 반영 검증을 완료했다. 이 영상과 메모는 사용자가 추가했으며 에이전트가 생성하거나 변경하지 않았다.

## 후속 확인 — 독립 게스트 권한

- 기존 Chrome의 Google 세션과 분리된 인앱 브라우저에서 운영 게스트 로그인을 수행했다. 게스트 배지와 비어 있는 영상·컬렉션 목록을 확인했다.
- 게스트로 `/admin`에 접근하면 `접근 권한이 없습니다` 대화상자와 관리자 전용 안내만 표시됐다. `확인`을 누르면 직전 `/dashboard`로 돌아왔다.
- 기존 Google 계정 소유의 비공개 테스트 영상 `/videos/f3yzzSozalbvzWDBRDH0`에 게스트로 직접 접근하면 404 안내가 표시됐으며 영상·메모는 노출되지 않았다.
- 게스트 세션의 관리자 API를 브라우저로 직접 열려는 요청은 클라이언트에서 차단됐다. API 서버의 응답 코드 검증으로 계산하지 않는다. 기존 1차 검증의 비인증 403 응답과 구분한다.
- 운영 익명 인증 계정 1개를 생성했고 이어서 사용할 수 있도록 게스트 탭을 유지했다. 이 게스트에는 영상·메모·컬렉션을 추가하지 않았으며 Google 연결이나 데이터 이관도 아직 수행하지 않았다. 기존 Chrome 로그인 상태·운영 데이터·지표 동의는 변경하지 않았다.

이 단계에서는 Google 연결·이관 대상을 임의로 선택하지 않고 사용자 확인을 기다렸다. 일반 Google 비관리자 계정의 접근 차단과 Firestore 규칙 전체를 검증한 것은 아니다.

## 후속 확인 — 기존 Google 계정 이관 시도에서 팝업 차단

사용자가 현재 로그인된 Google 계정으로 테스트하고 데이터를 합쳐도 된다고 승인했다. 인앱 브라우저와 Chrome은 Google 로그인 세션을 공유하지 않으므로 Chrome에서 기존 웹 계정을 로그아웃하고 새 운영 게스트 세션을 시작했다. 앞서 만든 인앱 브라우저의 빈 게스트와는 별개다.

- 새 게스트 영상: `/videos/NQvTo0Ueh861DB2Wabkq` (YouTube `M7lc1UVf-VE`).
- 새 게스트 메모 1개: 0:00, `[계정 이관 검증 2026-09-13] 게스트에서 작성 — 기존 Google 계정 연결 후 보존 확인`.
- 계정 연결 전 새로고침으로 게스트 배지와 영상·메모의 저장 유지를 확인했다.
- `Google로 계정 연결` 클릭 후 브라우저 콘솔에서 `계정 연결 실패: FirebaseError: Firebase: Error (auth/popup-blocked).`가 기록됐다.
- 화면에는 오류 안내가 표시되지 않았고, 새로고침 후에도 게스트 배지·연결 버튼과 동일한 테스트 메모가 유지됐다. 이관 성공으로 계산하지 않는다.
- 수정 전 소스는 `auth/credential-already-in-use` 처리 중 두 번째 `signInWithPopup`을 호출했다 (`src/lib/firebase/auth.ts`). 이 비동기 재팝업 경로가 원인 후보이나, 이번 관찰만으로 첫 번째와 두 번째 팝업 중 어느 호출이 차단됐는지는 확정하지 않는다.
- 수정 전 `UserMenu`는 연결 실패를 콘솔에만 출력해 사용자에게 실패·재시도 안내를 제공하지 않았다.

데이터 접근을 유지하기 위해 Chrome의 새 게스트 세션에서 로그아웃하지 않았다. 기존 Google 계정의 데이터는 삭제·수정하지 않았으며 이관 완료도 확인되지 않았다. 새 게스트 영상·메모와 두 브라우저의 게스트 세션을 유지했다. 기존 Google 계정으로 웹 로그인 상태를 복원하지 않은 상태임에 유의한다. 팝업 차단 설정을 변경하거나 브라우저 제한을 우회하지 않았다.

## 로컬 수정 — 재팝업 제거와 실패 복구 (배포 전 기록)

- 기존 Google 계정과 충돌하면 `GoogleAuthProvider.credentialFromError`에서 받은 자격증명으로 `signInWithCredential`을 호출한다. 비동기 두 번째 팝업은 제거했다. 새 Google 계정 연결은 기존 `linkWithPopup` 경로를 유지한다.
- 서버가 익명 세션 쿠키로 이관 원본을 결정하는 권한 검증은 변경하지 않았다. 데이터 이관 성공 후에만 Google 서버 세션을 생성한다.
- 이관 실패 시에는 이관부터, 이관 후 세션 갱신 실패 시에는 세션 갱신부터 재시도한다. 새로고침 뒤 Firebase 인증 상태 복원을 기다리는 처리도 포함했다. 클라이언트 진행 상태에 OAuth 자격증명은 저장하지 않는다.
- 연결 중 중복 클릭과 로그아웃을 막고, 팝업 차단·취소·네트워크 오류·기타 실패에 대한 한국어/영어 대화상자와 재시도 버튼을 추가했다. Firebase 오류 전체를 콘솔에 출력하지 않는다.
- 인증 제어 흐름과 오류 분류는 단위 테스트로 검사한다. 격리 브라우저 테스트는 Google API 로더를 차단해 실제 실패 안내·재시도·닫기·게스트 쿠키 유지 및 이관/세션 API 미호출을 검사한다. 실제 Google OAuth 성공이나 운영 데이터 이관을 대신하는 테스트가 아니다.

구현 근거: [Firebase GoogleAuthProvider API](https://firebase.google.com/docs/reference/js/auth.googleauthprovider), [Firebase 계정 연결 안내](https://firebase.google.com/docs/auth/web/account-linking).

최종 로컬 검증: 단위 테스트 143개, Firestore 에뮬레이터 테스트 32개, PC·모바일 브라우저 테스트 6개 통과. `yarn lint`, `yarn typecheck`, `yarn format:check`, `yarn build:ci`, `yarn --cwd extension build`, `git diff --check`도 통과했다. 기존 `metadataBase` 및 Vite CJS 사용 중단 예고 경고는 남아 있다. 테스트 중 영어 대화상자의 중복 Close 버튼을 하나로 정리했다.

이 단계에서는 운영 배포와 배포 후 재검증이 필요했다. 이후 결과는 아래 후속 확인에 기록한다. 첫 팝업 자체가 브라우저에서 차단되는 경우는 재팝업 제거와 별개의 브라우저 설정 문제일 수 있으며, 이제 실패 안내로 확인할 수 있다.

## 후속 확인 — 배포 후 기존 Google 계정 이관 성공

- CI 동의 철회 테스트를 보강한 `df08aff`의 [CI 실행](https://github.com/smw0807/yttm/actions/runs/34741412811)이 통과했고, 사용자가 운영 배포 완료를 알렸다.
- Chrome에서 보존한 게스트 영상 `/videos/NQvTo0Ueh861DB2Wabkq`와 0:00 이관 테스트 메모를 다시 확인한 뒤 `Google로 계정 연결`을 실행했다.
- 게스트 표시가 기존 Google 계정 이름으로 바뀌었으며, 영상·메모가 그대로 유지됐다. 새로고침 후에도 동일한 계정과 메모를 확인했다.
- 기존 계정의 `/videos/f3yzzSozalbvzWDBRDH0`에 있는 0:07 운영 검증 메모, `/videos/sYo20rsOyS8RcemsgO7R`의 0:16 `교전` 메모도 유지됐다. 검증한 세 영상은 같은 Google 계정에서 접근 가능했다.
- 이 결과는 해당 기존 계정 및 영상·메모 샘플의 이관 검증이다. 모든 데이터 유형과 실패 복구 경로를 운영에서 재현한 것은 아니다.

## 후속 확인 — 일반 Google 계정의 운영 권한 차단

- 사용자가 지정한 비관리자 Google 계정을 별도 Chrome `Minu` 프로필에서 사용했다. Google 계정 표시와 YTTM의 `Minu Song` 이름 및 빈 대시보드를 확인했다. 기존 관리자 프로필은 관리자 로그인 상태로 유지했다.
- `/dashboard`에서 `/admin`으로 이동했을 때 관리자 전용 `접근 권한이 없습니다` 안내를 확인했다. 관리자 통계나 사용자 목록은 표시되지 않았고 `확인` 클릭 후 `/dashboard`로 돌아왔다.
- 최초 관찰에서는 빈 화면이 보였으나 새로고침과 후속 확인에서 대화상자가 정상 표시됐다. 해당 원인을 확정하지 않으며, 콘솔에서는 이 페이지의 오류가 관찰되지 않았다.
- 같은 비관리자 프로필에서 `/api/admin/users/permission-check`를 직접 요청했다. 본문 `{"error":"Forbidden"}`과 브라우저 콘솔의 해당 URL에 대한 HTTP 403 응답을 확인했다. `permission-check`는 접근 차단을 검사하기 위한 가상 대상 식별자다.
- 기존 관리자 계정 소유의 비공개 영상 `/videos/f3yzzSozalbvzWDBRDH0`는 404 안내로 차단됐으며 영상·메모는 노출되지 않았다. 확인 후 비관리자 대시보드로 복귀했다.
- 이 검증에서는 운영 영상·메모·컬렉션을 생성·수정·삭제하거나 지표 동의를 변경하지 않았다. 비관리자 프로필의 로그인은 유지했다.

## 자동 회귀 검증 — 관리자 권한 경계

- `tests/e2e/admin-access.spec.ts`에 Google 제공자 비관리자·게스트·비인증 시나리오를 추가했다. 한국어 PC와 영어 모바일 크기에서 실행한다.
- Google 제공자 세션은 고정 demo 프로젝트의 Auth 에뮬레이터에서 합성한 자격증명으로 생성하고, 실제 앱 세션 API를 통과시킨다. 세션의 `google.com` 제공자와 관리자 UID 불일치를 검증한다. 실제 Google OAuth의 대체 검증으로 계산하지 않는다. [Firebase 에뮬레이터 안내](https://firebase.google.com/docs/emulator-suite/connect_auth)
- 비관리자·게스트의 관리자 API 403, 사용자 목록·온보딩 통계 미노출, 접근 거부 확인 후 대시보드 복귀와 세션 유지, 비인증 로그인 이동을 검사한다. 관리자 세션의 같은 API가 실제 fixture 데이터를 반환하는 양성 대조군도 포함했다.
- 최종 로컬 검증: `E2E_PORT=3101 E2E_BROWSER_CHANNEL=chromium yarn test:e2e` 14개 통과, 단위 테스트 143개 통과. 린트·타입·포맷 검사 및 `git diff --check` 통과. 운영 권한 검증과 별도로 격리된 로컬 데이터만 초기화했다.

## 후속 확인 — 운영 규칙 불일치 및 환경 설정

### 배포된 Firestore 규칙: 조치 필요

Firebase CLI의 기존 로그인으로 Rules API의 release 목록과 ruleset 소스만 조회했다. 운영 데이터 조회·쓰기나 규칙 배포는 수행하지 않았다.

- 대상 프로젝트: `yttm-38af5`, release: `projects/yttm-38af5/releases/cloud.firestore`.
- 활성 ruleset: `13b4f99b-627a-42b3-af95-fb5212e18703`.
- release 갱신 시각: `2026-03-28T14:36:09.496042Z`.
- 운영 소스 SHA-256: `13100e70769269f108f4b17b9e323147f09cd25e0a7f39752a54ef0e40c34ce4`.
- 저장소 `firestore.rules` SHA-256: `7bb61dd5294aa9510b17650b57d44b5de6a739ee958d21ad6fe3808d93f9c67e`.
- 줄바꿈과 앞뒤 공백을 정규화해도 서로 다르다. 운영 규칙에는 저장소의 보안 강화가 반영되지 않았다.

운영 소스에서 확인한 차이:

1. `shareToken != null`인 영상과 하위 메모는 비인증 직접 읽기를 허용한다. 토큰을 제출하거나 알고 있는지 검사하지 않는다. 저장소 규칙은 공유 여부와 관계없이 직접 읽기를 소유자에게만 허용한다.
2. 영상·컬렉션의 기존 소유자는 `userId`를 다른 UID로 수정할 수 있다. 저장소 규칙은 업데이트 전후 소유자 UID가 같아야 한다.
3. 사용자 프로필 생성·수정에서 본문 `uid`의 일치·불변 조건이 없다. 저장소 규칙에는 해당 조건이 있다.

이는 활성 규칙 소스의 권한 분석이며, 운영 데이터로 무단 접근·소유자 변경을 재현하거나 실제 유출을 확인한 결과는 아니다. 기존 웹의 비공개 영상 404 확인만으로 Firestore 클라이언트 접근까지 안전하다고 판단할 수 없다.

웹 배포와 Firestore 규칙 배포는 별도다. [Firebase 규칙 배포 안내](https://firebase.google.com/docs/rules/manage-deploy)

### Vercel 환경 설정: 조회 완료

- Vercel의 YTTM Production 화면은 `main`의 `cceae347f00c12f141c57da6448a9a10585c0a79`를 Ready로 표시했다. 배포 ID는 `EZvMbNuaMVFDAsEPb8QJ9kEK9qr7`이다. 이후 develop의 `5428222` [CI 실행](https://github.com/smw0807/yttm/actions/runs/34742165228)도 성공했다. CI 성공을 운영 반영으로 계산하지 않는다.
- `ADMIN_UID`는 Production 범위의 Secret으로 등록돼 있다. Firebase 공개 설정 6개, `FIREBASE_ADMIN_SDK`, `YOUTUBE_API_KEY`, `NEXT_PUBLIC_BASE_URL`은 All Environments에 등록돼 있다.
- 공개 설정인 `NEXT_PUBLIC_BASE_URL=https://www.yttm.kr`, `NEXT_PUBLIC_FIREBASE_PROJECT_ID=yttm-38af5`를 확인했다. 비밀값은 공개·복사하지 않았으며, 마스킹된 값의 내용이나 현 배포에 주입된 값 전체까지 검증한 것은 아니다.
- 프로젝트 환경변수 목록에 Firestore/Auth 에뮬레이터 호스트나 `NEXT_PUBLIC_FIREBASE_EMULATORS` 항목은 없었다. 빌드 과정의 모든 환경변수 주입 경로까지 검사한 것은 아니다.
- 현재 소스에서 사용하지 않는 `ADMIN_UIDS`가 All Environments에 남아 있다. `YOUTUBE_API_KEY`와 `FIREBASE_ADMIN_SDK`는 Config 유형이며 Secret 유형 전환 검토가 필요하다. 이 관찰만으로 자격증명 유출을 의미하지 않는다.
- `NEXT_PUBLIC_ADFIT_UNIT`은 Development에만 있다. 운영 광고가 필요하다면 Production 범위 설정이 별도로 필요하다.
- 환경변수나 배포 설정은 변경하지 않았다.

### 추가 권한 회귀 검증: 로컬 완료

- Google·익명 제공자별로 비공개/공유 영상의 메모 목록 조회와 쓰기를 소유자에게만 허용하는지 검사했다. 비인증 메모 목록도 차단된다.
- 영상·컬렉션의 타인 UID 필터 조회 차단, 자신의 빈 목록 조회 허용, 타인 문서 쓰기가 섞인 batch 전체 거부 및 자신의 신규 문서 미생성을 검사했다.
- 부모 영상 삭제 후 남은 메모 접근 차단, 타인 프로필 조회·목록·수정·삭제 차단을 추가했다.
- `yarn test:emulator`: 3개 파일, **40개 테스트 통과**. 고정 `demo-yttm-tests` 로컬 에뮬레이터에서 저장소 규칙으로 수행했다. 운영 규칙의 통과 결과가 아니다.
- 현재 확장 소스는 자신의 UID로 영상 목록을 조회하고 프로필 생성 시 자신의 `uid`를 저장한다. 공유 웹 페이지는 서버의 토큰 조회 경로를 사용한다. 최신 규칙의 소유자 제한과 부합하는 코드 경로를 확인했지만 모든 구버전 확장의 호환성을 검증한 것은 아니다.

### 운영 규칙 배포 준비 — 승인 후 실행

에뮬레이터 설정과 분리한 `firebase.production.json`을 추가했다. 프로젝트와 배포 대상을 명시해 **규칙만** 배포하며 웹·인덱스·데이터는 배포 대상에 포함하지 않는다. 아래 명령은 아직 실행하지 않았다.

```sh
yarn firebase deploy --only firestore:rules --project yttm-38af5 --config firebase.production.json
```

배포 승인 후 확인 순서:

1. 배포 직전에 활성 ruleset을 재확인하고 현재 소스와 release 식별자를 보관한다.
2. 규칙만 배포한 뒤 활성 release의 소스가 저장소와 일치하는지 재조회한다. 전파 지연을 고려한다.
3. 로그인한 소유자의 영상·메모·컬렉션 조회와 확장 조회, 비인증 공유 링크 표시를 확인한다.
4. 테스트용 데이터 생성·정리가 승인된 범위에서 다른 계정의 직접 SDK 접근 차단 및 소유자 변경 거부를 확인한다. Admin SDK는 규칙을 우회하므로 이 검증의 클라이언트로 사용하지 않는다.
5. 정상 사용자 경로가 차단된다면 원인을 확인한다. 보안이 약한 이전 규칙으로 자동 롤백하지 않고 영향과 대응을 사용자에게 보고한다.

## 후속 확인 — 승인 후 Firestore 규칙 운영 배포 완료

사용자가 규칙만 운영에 배포하고 재검증하는 것을 승인했다. Vercel 웹 배포는 `main`의 `761f197` (PR #38 병합)을 Production Ready로 별도 확인했다.

- 배포 직전 기존 활성 release와 규칙 소스를 `/tmp/yttm-firestore-rules-backup.XgNKmu/release.json`에 백업했다. 임시 파일이며 영구 보관을 보장하지 않는다. 이전 ruleset은 `13b4f99b-627a-42b3-af95-fb5212e18703`이다.
- `yarn test:emulator`를 다시 실행해 3개 파일, 40개 테스트 통과 후 위의 규칙 전용 명령에 `--non-interactive`를 붙여 배포했다. 컴파일·업로드·release 갱신이 모두 성공했다.
- 새 활성 ruleset: `952b2237-901b-4576-8822-439586bd6907`.
- release 갱신 시각: `2026-09-13T06:34:54.780581Z` (한국 시각 15:34:54).
- Rules API로 활성 소스를 다시 읽어 저장소 `firestore.rules`와 **바이트 단위 일치**를 확인했다. 두 소스의 SHA-256은 `7bb61dd5294aa9510b17650b57d44b5de6a739ee958d21ad6fe3808d93f9c67e`이다.
- 기존 소유자 Google 세션으로 대시보드를 새로고침하고 영상·컬렉션 목록을 확인했다. `/videos/f3yzzSozalbvzWDBRDH0`에서 0:07 테스트 메모와 타임라인 1개를 확인했고, `/collections`에서 기존 목록을 확인했다.
- 자격증명 없는 Firestore REST 요청으로 위 영상 문서, 해당 메모 목록, 컬렉션 목록을 각각 조회해 HTTP 403 `PERMISSION_DENIED`를 확인했다. 응답 데이터나 인증 토큰은 기록하지 않았다.
- 쿠키 없는 공개 데모 공유 페이지 요청은 HTTP 200이며 플레이어 데이터와 타임라인 콘텐츠가 포함됐다. 실제 재생·장면 이동을 이번에 다시 검증한 것은 아니다.
- 운영 데이터 생성·수정·삭제, 환경변수 변경, 추가 웹 배포는 수행하지 않았다. 규칙만 변경했다.

검증 범위: 웹 조회는 서버 경로도 포함하므로 모든 클라이언트 SDK 경로의 성공을 의미하지 않는다. 배포 후 설치된 확장의 직접 조회, 실제 두 계정의 SDK 접근, 소유자 변경·일괄 쓰기 차단의 운영 재현은 남아 있다. 쓰기 거부는 로컬 에뮬레이터 결과와 배포 소스 일치로 확인했으며 운영 쓰기 테스트로 계산하지 않는다.

## 후속 확인 — 규칙 배포 후 설치된 확장 조회

- 기존 관리자 Google 계정을 유지한 Chrome 프로필에서 검증용 YouTube 영상 `M7lc1UVf-VE`를 새 탭으로 열었다.
- 설치된 확장 `akcenlcmjliffoildhkhmpmeiebnaiak`의 팝업에서 로그인 계정 표시를 확인하고 사이드패널을 열었다. 확장 재설치·로그아웃·권한 변경은 하지 않았다.
- 사이드패널의 로딩 표시가 사라진 뒤 영상 제목과 0:00의 기존 `[계정 이관 검증 2026-09-13]` 메모 본문이 표시됐다. 메모 입력란은 비어 있고 저장 버튼은 비활성 상태였다.
- 현재 저장소의 확장 조회 경로는 Firebase 클라이언트 SDK의 소유자 UID 필터 영상 조회와 메모 구독이다. 설치된 확장 화면의 정상 표시는 확인했지만 설치 버전·번들 일치, 네트워크 응답과 캐시의 구분, 모든 CRUD 동작까지 검증한 것은 아니다.
- 검증용으로 연 YouTube 탭은 닫았다. 운영 영상·메모·컬렉션을 생성·수정·삭제하지 않았고 로그인 상태를 유지했다.
- 실제 두 계정의 직접 SDK 권한 검증은 미완료다. 확인한 Chrome 창 목록에서 앞서 사용한 비관리자 YTTM 창을 찾지 못해 사용자에게 해당 프로필의 YTTM 창을 다시 열어 달라고 요청했다. 다른 계정으로 임의 전환하거나 인증 토큰을 추출하지 않았다.
- 사용자가 비관리자 Chrome 창을 다시 열었다고 알렸지만, 자동화 도구는 기존 창 메뉴만 반환했다. 최신 상태 재조회·앱 재연결·브라우저 제어 세션 초기화 후에도 같았고, 스크린샷은 사용할 수 없었다. 비관리자 계정의 로그아웃이나 제품 오류로 판단하지 않으며, 새 창의 로그인 상태를 확인한 것으로 계산하지 않는다.

### 후속: 비관리자 창 연결 복구 및 웹 조회 대조

- 사용자가 창을 앞으로 가져온 후 Chrome `Minu` 프로필의 Google 계정과 YTTM의 `Minu Song` 로그인 표시를 확인했다. 앞선 창 인식 제약은 해소됐다.
- 비관리자 대시보드에 영상 1개가 표시됐다. `/videos/zhFS9QgjKd2JXx55IlGO`에서 해당 계정의 영상과 타임라인 2개(34:22, 1:23:14)를 확인했다. 이 데이터는 에이전트가 이번에 생성한 것이 아니다.
- 동일 세션에서 관리자 계정의 `/videos/f3yzzSozalbvzWDBRDH0`에 접근하면 404 안내가 표시됐고 영상·메모가 노출되지 않았다.
- 네이티브 접근성 텍스트가 이전 페이지를 반환하는 경우가 있어, 소유 영상의 타임라인과 타인 영상의 404는 스크린샷으로 확인했다. 데이터 변경 없이 대시보드 이동을 요청했다. 이후 전면 창이 다른 작업으로 전환되어 복귀 완료 화면은 확인하지 않았다.
- 이 결과는 실제 비관리자 세션의 웹 서버 경로 검증이다. Firestore 직접 SDK 요청을 수행한 결과로 계산하지 않는다.

## 후속 확인 — 읽기 전용 SDK 검증 도구 및 계정 A 실측

- `yarn verify:firestore:live --project yttm-38af5`로 실행하는 로컬 검증 도구를 추가했다. [사용 절차와 안전장치](./firestore-access-verification.md)를 참고한다. 운영 웹에 진단 경로를 배포하지 않는다.
- Google 로그인을 별도의 메모리 전용 Firebase 앱으로 수행한다. 관리자 자격증명이나 추출한 토큰을 사용하지 않으며, 데이터 쓰기 함수를 포함하지 않는다.
- 대상 A: `f3yzzSozalbvzWDBRDH0`, 대상 B: `zhFS9QgjKd2JXx55IlGO`. 두 대상은 앞서 각 소유자의 운영 웹에서 존재를 확인했다.
- 계정 A의 실제 Google 로그인 후 Firebase 클라이언트 SDK 서버 읽기 **9개 항목 모두 PASS**를 확인했다. 본인 영상 존재·소유권, 본인 메모·영상·컬렉션 목록 조회가 성공했다. 타인 영상·메모, 소유자 필터 없는 영상·컬렉션 목록, 서버 전용 지표 조회는 `permission-denied`였다.
- 계정 B의 별도 Chrome 프로필에서도 로컬 검증 화면을 열고 같은 대상 ID를 입력했다. 해당 Google 계정 선택 후 추가 인증 경로로 이동했고, 이후 네이티브 창 상태 조회가 시간 초과돼 사용자에게 인증 완료를 요청했다. **계정 B의 SDK 검증은 아직 실행 완료되지 않았다.**
- 사용자는 인증이 완료된 것 같다고 응답했다. 이후에도 네이티브 창 조회 시간 초과가 계속되어 역할 B 선택·검증 실행 및 결과 확인을 사용자에게 요청했다. 로그인 완료와 PASS 결과를 직접 확인한 것으로 기록하지 않는다. 계정 A의 로컬 검증 로그인은 해제했고 결과 화면은 유지했다. 후속 확인을 위해 로컬 서버를 실행 상태로 남겨 둔다.
- 도구 안전장치 단위 테스트 12개를 포함한 전체 단위 테스트 155개, 타입 검사, 변경 코드 린트, `git diff --check`가 통과했다. 로컬 서버는 정상 페이지 200, 허용하지 않은 Host 403, POST 405, `.env.local` 경로 404를 확인했다.
- 이번 작업에서 운영 규칙·환경 설정·영상·메모·컬렉션을 변경하지 않았다. 실제 OAuth 로그인은 수행했으며 Google/Firebase의 로그인 기록은 남을 수 있다.

## 후속 확인 — 계정 B 결과 수신 및 양방향 읽기 검증 완료

- 사용자가 제공한 첫 스크린샷에서 비관리자 Google 로그인은 완료됐지만 역할이 A로 선택돼 있었다. A 소유 영상의 양성 대조 조회가 `permission-denied`로 중단된 것이므로, 역할을 B로 바꾸고 다시 실행하도록 안내했다.
- 이후 사용자가 제공한 결과 스크린샷에서 비관리자 로그인, 역할 B, 앞서 사용한 동일한 A/B 영상 문서 ID를 확인했다. **계정 B의 9개 항목 모두 PASS**였다. 본인 영상 존재·소유권과 메모·영상·컬렉션 목록 조회가 성공했고, 타인 영상·메모, 소유자 필터 없는 영상·컬렉션 목록, 서버 전용 지표 조회는 모두 `permission-denied`였다.
- 계정 A는 에이전트가 브라우저에서 직접 확인했고, 계정 B는 사용자가 실행한 결과 스크린샷으로 확인했다. 두 계정 합계 **18개 항목 통과**로 해당 두 영상에 대한 양방향 읽기 권한 검증을 완료했다.
- 화면의 “다른 역할의 계정도 검증해야 전체 완료”는 결과가 각 Chrome 프로필의 탭 메모리에 따로 저장되기 때문에 표시된다. 동일한 대상에 대한 두 프로필의 결과를 함께 대조했으며 한 탭의 자동 전체 통과 표시를 확인했다고 주장하지 않는다.
- 운영 데이터 쓰기·소유자 변경·배치 쓰기는 수행하지 않았다. 해당 경로의 검증 근거는 기존 에뮬레이터 테스트이며, 이번 운영 읽기 검증 결과와 구분한다. 전체 운영 검증 완료를 의미하지 않는다.
- 결과 기록 후 로컬 검증 서버를 종료하고 4318 포트의 리스너가 없는 것을 확인했다. 계정 B의 검증 탭은 사용자가 닫으면 메모리 전용 로그인도 해제된다. 운영 웹의 로그인 상태는 변경하지 않는다.

## 환경변수 정리 점검 — 2026-09-13

사용자가 웹 배포를 다음으로 미뤄 이번 단계에서는 Vercel 등록 상태와 소스 사용처를 대조했다. **운영 환경변수 저장·삭제·키 교체·웹 배포는 수행하지 않았다.**

| 변수                                  | 현재 Vercel 유형 / 범위   | 확인 및 후속 조치                                                             |
| ------------------------------------- | ------------------------- | ----------------------------------------------------------------------------- |
| `ADMIN_UID`                           | Secret / Production       | 서버 세션의 Firebase UID 1개와 비교. 현재 설정 유지                           |
| `FIREBASE_ADMIN_SDK`                  | Config / All Environments | 서버에서 Base64 디코딩 후 서비스 계정 JSON으로 사용. Secret 전환 승인 필요    |
| `YOUTUBE_API_KEY`                     | Config / All Environments | YouTube API 서버 라우트에서 사용. Needs Attention 표시, Secret 전환 승인 필요 |
| `ADMIN_UIDS`                          | Config / All Environments | 현재 `src`에서 미사용. 삭제 승인 필요                                         |
| Firebase 공개 설정 6개                | Config / All Environments | `src/lib/firebase/config.ts`에서 사용. 현 상태 유지                           |
| `NEXT_PUBLIC_BASE_URL`                | Config / All Environments | SEO·sitemap·robots에서 사용. 현 상태 유지                                     |
| `NEXT_PUBLIC_NAVER_SITE_VERIFICATION` | Config / All Environments | 메타데이터에서 사용. 현 상태 유지                                             |
| `NEXT_PUBLIC_ADFIT_UNIT`              | Config / Development      | 운영 범위 미등록. 운영 광고 활성화 여부 미결정, 임의 추가하지 않음            |

- `YOUTUBE_API_KEY` 편집 화면에서 기존 값을 재입력하지 않고 Config/Secret 유형을 선택하는 UI를 확인했다. Secret을 임시 선택한 뒤 **Cancel**로 종료했으며 저장 성공을 검증한 것은 아니다. 값은 로그·문서에 기록하지 않았다.
- 최신 [Vercel 변경 안내](https://vercel.com/changelog/environment-variables-now-use-config-and-secret-types)에 따르면 Secret은 저장 후 값을 다시 조회할 수 없다. 전환 전 소유자가 안전하게 관리하는 원본이 있는지 확인한다. Config 등록이나 Needs Attention 경고만으로 유출이 입증되는 것은 아니다.
- 승인 시 제안 범위는 위 서버 비밀값 2개의 **기존 값과 적용 범위를 유지한 Secret 전환** 및 미사용 `ADMIN_UIDS` 삭제다. 삭제 전 이전 소스를 다시 빌드하는 Preview/롤백 절차에서 해당 변수를 필요로 하지 않는지 확인한다. Secret 전환을 자격증명 교체로 간주하지 않으며, 키 교체·환경별 자격증명 분리·팀 정책 변경은 별도 작업이다. 저장 시 범위 제한이나 키 재입력을 요구하면 중단하고 확인한다.
- All Environments에 서버 비밀값이 등록돼 있으므로 환경별 자격증명 분리는 후속 보안 검토 대상이다. 이번 점검은 Preview/Development 권한이나 연결 프로젝트를 변경하지 않는다.
- 현재 광고 호출부는 `unit`을 직접 지정하지 않고 환경변수를 사용한다. `NEXT_PUBLIC_ADFIT_UNIT`이 없는 빌드에서는 배너를 렌더링하지 않는다. 이번 관찰은 설정 목록과 소스 기준이며, 기존 운영 빌드의 광고 노출을 새로 검증한 결과는 아니다.
- README의 이전 AdSense 설정을 실제 Kakao AdFit 설정으로 교체하고 `ADMIN_UID`, 네이버 검증 설정을 보완했다. Firebase 서비스 계정 형식을 Base64로 바로잡고 두 환경변수 예제를 일치시켰다.
- [Vercel 문서](https://vercel.com/docs/environment-variables)에 따라 환경변수 변경은 기존 배포에 소급 적용되지 않는다. 다음 웹 배포 때 로그인, YouTube 조회, 관리자/비관리자 접근을 확인해야 한다.

## 구버전 확장 인증 API 보강 — 2026-09-13

- 현재 `extension/src/lib/auth.ts`는 Google credential로 Firebase `signInWithCredential`을 직접 호출한다. 현재 웹·확장 소스에서 `/api/auth/extension-token` 호출부는 찾지 못했다. **운영 트래픽과 배포된 구버전 확장의 사용 여부는 확인하지 않았으므로 API를 삭제하지 않았다.**
- 기존 `{ accessToken }` → `{ customToken }` 계약과 확장용 CORS/OPTIONS는 유지했다. CORS는 인증 수단이 아니며, Firebase가 해석한 Google 사용자 UID만 토큰 발급에 사용한다. 요청의 `uid`는 사용하지 않는다.
- JSON Content-Type, 객체 형태, 비어 있지 않은 문자열 토큰(최대 4,096자, 공백 불가)을 검사한다. 본문은 헤더 유무와 무관하게 스트림 기준 8,192바이트로 제한하며 초과 시 읽기를 취소한다. 제한값은 이 호환 API의 정책이며 Google 토큰 길이의 공식 상한을 의미하지 않는다.
- 기존 Firestore 트랜잭션 기반 제한기를 재사용한다. 인증 전 전체 API 예산은 60초 창당 120회, 인증 완료 후 UID별 발급 시도는 60초 창당 10회다. 초과 시 429와 Retry-After를 반환하고 제한기 장애 시 503으로 발급을 중단한다. 원문 토큰/IP를 제한 키로 저장하지 않는다.
- 전체 예산은 익명 요청의 Firebase 호출을 제한하지만 공격자가 소진하면 정상 구버전 로그인도 해당 창 동안 제한된다. 또한 거절된 요청도 제한기 조회 비용이 발생하므로 DDoS/비용 보호 전체를 해결한 것은 아니다. 운영 트래픽 확인 후 임계값과 플랫폼 방화벽 필요성을 검토한다. 현재 확장의 직접 Firebase 로그인 경로는 이 예산을 사용하지 않는다.
- Firebase 요청에 10초 제한과 no-store를 적용했다. 응답도 성공·실패 모두 no-store이며, upstream 오류 본문과 예외 객체는 응답·로그에 노출하지 않는다. 불필요한 IdP 자격증명 반환 요청을 제거했다.
- Firebase가 성공 상태를 반환해도 Google provider, 유효한 UID와 ID token의 존재를 확인하고 계정 연결 확인/MFA 대기/오류 응답에서는 커스텀 토큰을 발급하지 않는다. 응답 필드 의미는 [Identity Platform signInWithIdp 문서](https://docs.cloud.google.com/identity-platform/docs/reference/rest/v1/accounts/signInWithIdp)를 근거로 했다. 실제 MFA 로그인 지원을 추가한 것은 아니다.
- 검증은 Firebase·네트워크·제한기를 모킹한 단위 테스트로 수행한다. 운영 API 호출, 실제 Google credential 교환, 운영 카운터 쓰기, 웹 배포는 수행하지 않았다. 이 변경은 다음 커밋·배포 대상이다.
- 결과: 새 API 테스트 50개를 포함한 전체 단위 테스트 205개 통과, 웹·확장 타입 검사, 변경 코드 ESLint, 포맷과 diff 검사 통과. 임시 자격증명을 사용하는 `yarn build:ci`도 성공했으며 이 빌드 결과는 배포하지 않는다. 빌드 중 `metadataBase` 미설정 경고가 있었고 이번 인증 API 작업에서는 수정하지 않았다. E2E와 운영 구버전 로그인은 이번 단계에서 재검증하지 않았다.

## 남은 우선순위

1. 환경변수 정리 적용 승인: 서버 비밀값 2개 Secret 전환과 미사용 `ADMIN_UIDS` 삭제. 점검·문서/예제 수정은 완료했고 운영 변경은 대기 중이다. 광고는 현 상태 유지, 활성화 여부는 별도 결정한다.
2. 구버전 확장 인증 API 보강분 커밋·배포 및 실제 호환성 확인. 입력 검증·호출 제한 보강은 완료했다. 운영 요청량과 구버전 사용 여부를 확인한 뒤 유지/폐기를 결정한다.
3. 모바일 실기기·Safari와 공유 생성/폐기·컬렉션 편집 등 추가 시나리오 검사. 운영 쓰기 테스트가 필요하면 데이터 범위를 먼저 합의한다.

이 기록만으로 전체 운영 검증 완료를 선언하지 않는다.
